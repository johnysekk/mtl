{
  "name": "mtl-native",
  "version": "1.0.0",
  "private": true,
  "description": "Native (Capacitor) shell for MTL — loads app.martialtraininglab.com, adds Tap to Pay.",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap open ios",
    "android": "cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor-community/stripe-terminal": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
